# Core functionality:
class TesseractOCR:
    async def extract_text(file_path: str) -> OCRResult:
        # Extract text from PDF
        # Calculate confidence per page
        # Return structured result
        
    async def extract_with_layout(file_path: str) -> LayoutResult:
        # Preserve positioning for line item extraction